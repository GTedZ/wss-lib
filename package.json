{
  "name": "wss-lib",
  "version": "0.0.4",
  "description": "The main priority is just to maintain a stable WS connection (which means it reconnects if the connection is lost), this is in no way shape or form intended for professional use, this is created just for the sake of not having to rewrite or handle the disconnections and having to reconnect to the server.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GTedZ/wss-lib.git"
  },
  "keywords": [
    "websocket",
    "ws"
  ],
  "author": "GTedZ",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/GTedZ/wss-lib/issues"
  },
  "homepage": "https://github.com/GTedZ/wss-lib#readme",
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0"
  }
}
